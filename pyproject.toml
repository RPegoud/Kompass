[project]
name = "kompass"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "Ryan Pégoud", email = "ryan.pegoud@outlook.com" }
]
requires-python = ">=3.14"
dependencies = [
    "hydra-core>=1.3.5",
    "ipykernel>=7.3.0",
    "modal>=1.5.4",
    "nbformat>=5.11.1",
    "ninja>=1.13.2",
    "numpy>=2.5.2",
    "pandas>=3.0.6",
    "plotly>=7.1.0",
    "pyright>=1.1.411",
    "rich>=15.0.0",
    "torch>=2.13.0",
    "tqdm>=4.70.1",
    "triton>=3.7.1",
    "typer>=0.27.2",
]

[build-system]
requires = ["uv_build>=0.12.5,<0.13.0"]
build-backend = "uv_build"

[tool.uv.build-backend]
module-root = "src"

[tool.pyright]
include = ["src", "tests"]
typeCheckingMode = "standard"
pythonVersion = "3.12"
reportMissingTypeStubs = false
extraPaths = ["."]

[project.scripts]
kompass = "kompass.cli.main:app"
